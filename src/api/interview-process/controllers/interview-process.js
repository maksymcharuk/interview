'use strict';

/**
 * interview-process controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::interview-process.interview-process');
